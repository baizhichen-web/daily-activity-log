/* ============================================================
   海报墙「待建设」占位数据（首页）。
   为什么是 .mjs 而不是 .ts：这份数据**两端都要读** —— 浏览器（FlipbookDemo 渲染墙面）
   与构建期生成器（scripts/ai-native/generate.mjs 出 llms.txt/.md 副本）。
   Node 读 .ts 要靠类型剥离（Node 22.18+ 才默认开），CI 上 Node 版本稍旧就构建失败；
   放成纯 JS 模块两侧都能直接 import，不再依赖运行时的 TypeScript 支持。
   类型声明在同名 .d.mts 里（只给 TS 侧看）。
   ============================================================ */

/* 2026-09-20 白纸指令：**整个「待建设」栏目去掉**（09-14 曾"暂时清空"、09-20 上午又要求恢复，
   本次为最终决定）。
   数组留空 = 模块头（TO BE BUILT + 说明段）与占位海报一起不再渲染，墙面只剩文章。
   想再回来：把 09-13 版四条数据（git 历史 f1ec1e9）放回数组即可，模块随之复原。 */
export const POSTER_WALL = [];
