/* ============================================================
   2026-09-14 之后本文件只剩「语言」一个类型。
   ⚠️ 身份块/教育/关于/实习/项目/获奖/课程/自评/证明材料等**已迁出代码**，
      现住在 content/resume.{zh,en}.yml（Decap 后台「简历」集合可编辑），
      加载口在 src/lib/content.ts 的 RESUME。
   ⚠️ 海报墙数据搬去了 src/data/poster-wall.mjs —— 构建期生成器（Node）也要读它，
      放 .mjs 才不必依赖 Node 的类型剥离（Node 22.18+ 才默认支持读 .ts）。
      类型声明在 poster-wall.d.mts。
   ============================================================ */

export type Lang = "zh" | "en";
