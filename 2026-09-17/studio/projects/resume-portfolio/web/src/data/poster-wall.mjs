/* ============================================================
   海报墙「待建设」占位数据（首页）。
   为什么是 .mjs 而不是 .ts：这份数据**两端都要读** —— 浏览器（FlipbookDemo 渲染墙面）
   与构建期生成器（scripts/ai-native/generate.mjs 出 llms.txt/.md 副本）。
   Node 读 .ts 要靠类型剥离（Node 22.18+ 才默认开），CI 上 Node 版本稍旧就构建失败；
   放成纯 JS 模块两侧都能直接 import，不再依赖运行时的 TypeScript 支持。
   类型声明在同名 .d.mts 里（只给 TS 侧看）。
   ============================================================ */

/* ⚠️ 2026-09-14 白纸指令：**暂时清空**「待建设」四张占位海报（01 如切如磋 / 02 共燃窑火 /
   03 闽南奥德赛 / 04 萤野流声）。数组一空，FlipbookDemo 的「待建设」模块头
   （TO BE BUILT + 说明段）也随之不再渲染，墙面只剩文章。
   四条原始数据在 git 历史里（content/resume.yml 迁移前的版本），要恢复就把它们放回数组。
   字段：no 编号 · title 题 · date 日期 · accent 项目主色 · slug→内容系统条目 · desc 摘要 · body 展开态正文 */
export const POSTER_WALL = [];
